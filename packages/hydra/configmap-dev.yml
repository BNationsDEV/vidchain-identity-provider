apiVersion: v1
kind: ConfigMap
metadata:
  name: hydra-configmap
data:
  URLS_SELF_ISSUER: "https://dev.vidchain.net"
  URLS_CONSENT: "https://dev.vidchain.net/consent"
  URLS_LOGIN: "https://dev.vidchain.net/login"
  URLS_LOGOUT: "https://dev.vidchain.net/logout"
  SERVE_COOKIES_SAME_SITE_MODE: Strict
  SERVE_PUBLIC_CORS_ENABLED: "true"
  SERVE_PUBLIC_CORS_ALLOWED_ORIGINS: "*"
  SERVE_PUBLIC_CORS_DEBUG: "true"
  SERVE_TLS_ALLOW_TERMINATION_FROM: "10.0.0.0/8"
  WEBFINGER_OIDC_DISCOVERY_SUPPORTED_SCOPE: '[\"VerifiableIdCredential\"]'
